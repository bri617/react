"use client";
import styled from "styled-components";

const History = () => {
  return (
    <Container>
      <h2>History</h2>
      {/* Replace with real history items later */}
    </Container>
  );
};

export default History;

const Container = styled.aside`
  background-color: white;
  border-radius: 12px;
  padding: 16px;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
  height: 100%;
`;
